<section class="wooden-sticky-footer" data-section-id="{{ section.id }}">
  <div class="wooden-sticky-footer__inner">
    <div class="wooden-sticky-footer__info">
      {% if section.settings.title != blank %}
        <p class="wooden-sticky-footer__title">{{ section.settings.title }}</p>
      {% endif %}
      {% if section.settings.price_text != blank %}
        <span class="wooden-sticky-footer__price">{{ section.settings.price_text }}</span>
      {% endif %}
    </div>
    {% if section.settings.cta_label != blank %}
      <a class="wooden-sticky-footer__cta" href="{{ section.settings.cta_link }}">
        {{ section.settings.cta_label }}
      </a>
    {% endif %}
  </div>
</section>

{% schema %}
{
  "name": "Wooden Sticky Footer",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "Ready to order?"
    },
    {
      "type": "text",
      "id": "price_text",
      "label": "Price text",
      "default": "Starts at ₹12,999"
    },
    {
      "type": "text",
      "id": "cta_label",
      "label": "CTA label",
      "default": "Add to cart"
    },
    {
      "type": "url",
      "id": "cta_link",
      "label": "CTA link"
    }
  ],
  "presets": [
    {
      "name": "Wooden Sticky Footer",
      "category": "Product"
    }
  ]
}
{% endschema %}
