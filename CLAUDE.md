# CLAUDE.md - LiveShopifyProject

Read `AGENTS.md` first.

## Practical Notes

- Theme work happens in `woodenstreet-theme/`.
- App work happens in `woodenbeast/` and is out of scope unless explicitly requested.
- The root-level duplicate theme folders were removed to keep one storefront build only.
- The master theme stylesheet is `woodenstreet-theme/assets/wooden-master.css`.
- Run Shopify theme commands from `woodenstreet-theme/`.
- Do not touch deploy flow without explicit approval from Shubh.
